import * as React from 'react';
import { Box, Text } from '@chakra-ui/react';
export const AppLogo = () => {
  return (
    <Box>
      <Text color={'text.primary'} fontSize={'4xl'} fontFamily={'fantasy'}>
        Cloud Campaigns
      </Text>
    </Box>
  );
};
